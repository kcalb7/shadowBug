const express = require('express')
// const service = require('../modules/user/service')
const router = express.Router()
// const { ensureAuthentication } = require('../middlewares')

// router.route('/').get(ensureAuthentication, service.list).post(ensureAuthentication, service.save)
// router
//   .route('/:id')
//   .get(ensureAuthentication, service.get)
//   .put(ensureAuthentication, service.edit)
//   .delete(ensureAuthentication, service.delete)

module.exports = router
